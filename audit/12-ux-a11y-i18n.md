# Phase 12 — UX, Accessibility & i18n

## 1. Internationalization

Library: i18next + react-i18next + browser language detector (`frontend/src/i18n.ts`).

Registered locales:

```9:16:frontend/src/i18n.ts
const resources = {
  en: { translation: enTranslations },
  ar: { translation: arTranslations },
}
```

Files on disk:
- `frontend/src/i18n/locales/en/translation.json`
- `frontend/src/i18n/locales/ar/translation.json`
- `frontend/src/i18n/locales/uk/translation.json` — **not imported**

Default locale env: `VITE_DEFAULT_LOCALE=en` (`docker-compose.yml:365`).

Separate Arabic landing: `frontend/src/views/LandingAr.tsx`.

### F-007 — Ukrainian locale orphaned (P2)

uk translation file exists but is not registered in `i18n.ts:9-16`.

### i18n XSS note

```25:27:frontend/src/i18n.ts
    interpolation: {
      escapeValue: false,
    },
```

**ASSUMPTION:** Safe if all interpolated values are trusted; risky if user-generated strings passed to `t()` — confidence MEDIUM.

## 2. Accessibility

**Search:** `grep "aria-label|aria-labelledby" frontend/src/**/*.tsx` → **120+ matches** across dashboard, CDP, campaigns, auth, primitives.

Primitive components document ARIA + keyboard support in `CLAUDE.md:198-199`.

Examples:
- `Sidebar.tsx` — nav aria labels
- `DataTable.tsx`, `pagination.tsx` — table navigation
- `EmergencyStop.tsx`, `TrustGateIndicator.tsx` — critical controls labeled
- `Login.tsx`, `Signup.tsx` — form accessibility

**Not audited:** full WCAG 2.2 automated scan — no axe/playwright a11y suite found in e2e specs.

## 3. Error & Loading UX

- `ErrorBoundary.tsx` + Sentry capture
- `LoadingSpinner.tsx` with aria (`LoadingSpinner.tsx` grep)
- `ErrorScreen.tsx` with aria
- React Query used for server state — loading/error patterns per view (not uniformly verified)

## 4. Theme / RTL

- Dark/light theme via `ThemeProvider` + `ThemeToggle`
- Arabic locale + `LandingAr` suggest RTL awareness — **full RTL CSS audit not performed**

## 5. UX Findings

| ID | Sev | Title |
|----|-----|-------|
| F-007 | P2 | uk locale file not wired |
| — | P2 | i18n escapeValue:false — review user-facing interpolations |
| — | P3 | No automated a11y regression suite in CI |

## 6. Positive Controls

- Radix UI primitives (accessible by default)
- Dedicated primitive test files include keyboard/ARIA checks (e.g. `Sidebar.test.tsx`, `ThemeToggle.test.tsx`)
- DOMPurify for user HTML content
- Headless UI / Radix for dialogs, menus

## 7. Searches Run

```
glob frontend/src/i18n/locales/*/translation.json  → en, ar, uk
read frontend/src/i18n.ts
grep "aria-label" frontend/src/**/*.tsx             → 120+ files
grep "axe|jest-axe|@axe-core" frontend/             → 0 matches
glob frontend/e2e/*.ts                              → no dedicated a11y spec
```

## 8. Pre-Release a11y Checklist

- [ ] Run axe DevTools on Login, Dashboard, CDP, Checkout
- [ ] Verify keyboard path to Emergency Stop and Trust Gate panel
- [ ] Test Arabic locale + RTL layout on primary dashboard
- [ ] Wire uk locale or remove unused file
- [ ] Audit `t()` calls with dynamic user content
