# =============================================================================
# Stratum AI - Backend Dependencies
# =============================================================================

# FastAPI Framework
fastapi==0.141.1
uvicorn[standard]==0.52.3
python-multipart==0.0.32
starlette==1.6.0

# Async Support
anyio==4.14.2
httpx==0.28.1
aiofiles==25.1.0
aiohttp==3.14.3
aiosmtplib==5.1.2
sendgrid==6.12.5
sse-starlette==3.4.8

# Database
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
psycopg2-binary==2.9.12
alembic==1.19.1
greenlet==3.5.5

# Redis & Caching
redis[asyncio]==8.1.0

# Celery
celery==5.6.3
flower==2.0.1
celery-redbeat==2.4.2
tzlocal==5.4.4  # required by celery.utils.time; pin explicitly so fresh installs don't drop it
msgpack>=1.2.1  # transitive via celery/kombu; <1.2.1 has GHSA-6v7p-g79w-8964 (OOB read on Unpacker reuse)

# Authentication & Security
PyJWT==2.13.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4's bcrypt backend requires bcrypt <4.1 (5.x breaks hashing)
cryptography==50.0.0
pydantic[email]==2.13.4
pydantic-settings==2.15.0
pyotp==2.10.0
qrcode[pil]==8.2

# ML / Data Science
scikit-learn==1.9.0
pandas==3.0.5
numpy>=2.4.6,<3.0
joblib==1.5.3

# Google Cloud (for Vertex AI)
google-cloud-aiplatform==1.164.0
google-auth==2.56.3

# LLM Integrations (Copilot)
anthropic==1.0.0
openai==3.0.0
pgvector==0.5.0

# Web Scraping (Module D)
beautifulsoup4==4.15.0
lxml==6.1.1

# Validation & Serialization
pydantic==2.13.4
orjson==3.12.0
email-validator==2.3.0

# Logging & Monitoring
structlog==26.1.0
python-json-logger==4.1.0
sentry-sdk[fastapi]==2.68.0
prometheus-client==0.26.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
respx==0.23.1
faker==40.36.0

# Development
black==26.5.1
isort==8.0.1
mypy==2.3.0
ruff==0.16.3

# Ad Platform SDKs
stripe>=15.5.1
requests>=2.34.2
facebook-business>=26.0.0
google-ads>=31.3.0

# Analytics & ML (scipy needed by ml/ab_testing.py)
scipy>=1.18.0

# Monitoring & Visualization
matplotlib>=3.11.1
prometheus-fastapi-instrumentator>=8.1.0
psutil>=7.2.2

# PDF & Reporting
weasyprint>=69.0
PyYAML>=6.0.3

# AWS (S3 for CMS assets & report delivery)
# boto3's real ceiling is set transitively, not here: aioboto3 15.5.0 pins
# aiobotocore[boto3]==2.25.1, which pins boto3<1.40.62. Any bump past that is
# unsatisfiable until aioboto3 ships a release built on a newer aiobotocore
# (the newest, 3.9.0, still caps botocore<1.43.57). See the dependabot ignore
# entry for boto3 in .github/dependabot.yml — drop both when upstream catches up.
boto3>=1.40.61
aioboto3>=15.5.0

# ML Explainability (optional, used by ml/explainability.py)
shap>=0.52.0

# Utilities
python-dotenv==1.2.2
tenacity==9.1.4
