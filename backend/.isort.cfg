[settings]
# Keep isort compatible with Black's formatting (CI runs both).
profile = black
# Mirror ruff.toml's exclusions — don't reorder auto-generated migration imports.
skip_glob = */migrations/*,*/alembic/*
